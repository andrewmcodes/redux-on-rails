# frozen_string_literal: true

RSpec.describe Redux::On::Rails do
  it "has a version number" do
    expect(Redux::On::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
